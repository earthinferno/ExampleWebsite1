'use strict';

import React from 'react';
import { Link } from 'react-router';

const Features = (props) => {

    return (
        <div>
            <div>
                <h1>FEATURES PAGE</h1>
                <p>We'll be adding some features shortly.</p>
            </div>
        </div>
    );

}

export default Features;